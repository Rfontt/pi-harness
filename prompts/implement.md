---
description: Implement in small, verifiable steps
---
If a workflow is active (phase=implement), apply tasks.md exactly: for each task, use edit(oldText=OLD, newText=NEW) with the verbatim snippets, run the task's Test, and verify its Done criterion. Do NOT re-read spec.md or plan.md, and do not edit files outside the tasks. If no workflow is active, follow the approved plan in small verifiable steps, running the corresponding verification after each.
